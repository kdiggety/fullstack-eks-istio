name: frontend-ci
on:
  pull_request:
    paths: ["frontend/**"]
  push:
    branches: [main]
    paths: ["frontend/**"]
jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install deps
        run: npm ci
        working-directory: frontend
      - name: Build
        run: npm run build
        working-directory: frontend
      - name: Docker build & push
        if: github.ref == 'refs/heads/main'
        run: |
          # TODO: build and push <ECR_OR_DOCKERHUB>/sample-frontend:${{ github.sha }}
        working-directory: frontend
